# This repository is connected to Vercel via the Vercel GitHub integration.
# Vercel automatically builds and deploys the project on every push to the
# `main` branch, so an explicit GitHub Actions workflow is not required.
# If you still want a custom CI step, you can add one here, but the
# deployment will work out‑of‑the‑box without any Vercel secrets.